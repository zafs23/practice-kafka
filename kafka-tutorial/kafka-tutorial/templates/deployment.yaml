apiVersion: apps/v1
kind: Deployment
metadata:
  name: zookeeper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zookeeper
  template:
    metadata:
      labels:
        app: zookeeper
    spec:
      containers:
        - name: zookeeper
          image: "{{ .Values.zookeeper.image }}"
          env:
          {{- range $env := .Values.zookeeper.environment }}
            - name: "{{ $env.name }}"
              value: "{{ $env.value }}"
          {{- end }}
          ports:
            - containerPort: {{ .Values.zookeeper.service.port }}

--- 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafkanew
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafkanew
  template:
    metadata:
      labels:
        app: kafkanew
    spec:
      containers:
        - name: kafkanew
          image: "{{ .Values.kafkanew.image }}"
          env:
            {{- range $env := .Values.kafkanew.environment }}
            - name: "{{ $env.name }}"
              value: "{{ $env.value }}"
            {{- end }}
          ports:
            - containerPort: 9092

--- 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: producer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: producer
  template:
    metadata:
      labels:
        app: producer
    spec:
      containers:
        - name: producer
          image: "{{ .Values.producer.image }}"
          imagePullPolicy: "{{ .Values.producer.imagePullPolicy }}"
          env:
            - name: KAFKA_BROKER
              value: "kafka:{{ .Values.kafkanew.service.port }}"
          ports:
            - containerPort: {{ .Values.producer.service.port }}

--- 
apiVersion: apps/v1
kind: Deployment
metadata:
  name: consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: consumer
  template:
    metadata:
      labels:
        app: consumer
    spec:
      containers:
        - name: consumer
          image: "{{ .Values.consumer.image }}"
          imagePullPolicy: "{{ .Values.consumer.imagePullPolicy }}"
          env:
            - name: KAFKA_BROKER
              value: "kafka:{{ .Values.kafkanew.service.port }}"
          ports:
            - containerPort: {{ .Values.consumer.service.port }}